You are working on an existing full-stack project (FedEx-Assistant).
MongoDB integration and OpenAI AI logic are ALREADY implemented and WORKING.

⚠️ DO NOT:
- Rewrite MongoDB connection logic
- Rewrite AI logic
- Remove any existing features
- Change environment variable names
- Break current API routes

🎯 GOAL:
Fix DATA FLOW and VISIBILITY issues only.

---

### TASK 1 — AI DATA VISIBILITY (BACKEND)
Currently, AI recovery scores are generated and logged in the console but NOT visible in the dashboard.

✅ Ensure these AI fields are always:
- Persisted in storage (already exists)
- Returned in API responses
- Used in dashboard statistics

Fields to expose everywhere:
- aiRecoveryScore
- aiLastUpdatedAt
- aiPriority
- aiFollowUpMessage

Specifically:
- GET /api/cases
- GET /api/cases/:id
Must include AI fields in the JSON response.

---

### TASK 2 — DASHBOARD AI METRICS
Enhance `getDashboardStats()` (do NOT replace it).

Add the following calculated fields:
- avgAiRecoveryScore (average of all non-null AI scores)
- lowRecoveryCases (count of cases where aiRecoveryScore < 40)

Return these along with existing stats.

---

### TASK 3 — AI + DATABASE STATUS ENDPOINT
Extend the existing endpoint:

GET /api/admin/ai-status

It should return:
{
  enabled: boolean,            // true if AI key exists
  aiMode: "openai" | "fallback",
  database: "MongoDB" | "Memory"
}

This endpoint is the SINGLE SOURCE OF TRUTH for:
- AI enabled/disabled
- AI fallback mode
- Active database

---

### TASK 4 — DASHBOARD DISPLAY FIX (FRONTEND)
Without redesigning the UI, update the dashboard to display:

- AI Status (Active / Fallback)
- Average AI Recovery Score
- Count of Low Recovery Cases (<40)
- Database Mode (MongoDB / Memory)

Use existing dashboard components.
DO NOT create new pages.
DO NOT remove charts.

---

### TASK 5 — DATA LOAD LOGIC CORRECTION
Fix restart behavior clarity:
- Do NOT reseed data if MongoDB already contains records
- Ensure seed runs ONLY when database is empty
- Ensure dashboard always fetches from API, never relies on console logs

---

### TASK 6 — SAFETY & STABILITY
- Handle OpenAI quota / rate-limit errors gracefully
- Fallback logic must continue working
- App must start even if AI fails
- No crashes allowed

---

### FINAL CHECKLIST (MUST PASS)
✅ AI scores visible in dashboard  
✅ AI stats visible in dashboard  
✅ AI + DB status visible  
✅ MongoDB data persists across restarts  
✅ No breaking changes  
✅ Console logs remain for debugging  

Proceed carefully, make minimal changes, and respect existing architecture.
