You are a senior full-stack engineer auditing and stabilizing an enterprise Node.js + Express + Vite project called FedEx-Assistant.

The repository is already imported from GitHub.
Do NOT rewrite the project. Do NOT remove existing logic.

Your task is to verify, fix, and standardize the project so it runs cleanly in:

Replit

VS Code (local)

Render / Railway (later)

1️⃣ ENVIRONMENT & SECURITY

Ensure .env is NOT committed

Create .env.example with:

PORT=5000
NODE_ENV=development
MONGODB_URI=your_mongodb_uri_here
OPENAI_API_KEY=your_openai_key_here


Load env safely using dotenv/config

Never hardcode secrets

2️⃣ DATABASE HANDLING (CRITICAL)

If process.env.MONGODB_URI exists:

Connect to MongoDB

Log: 🟢 MongoDB connected

Use MongoDB-backed storage

If MONGODB_URI does NOT exist:

Fall back to MemStorage

Log: 📦 Storage Mode: In-Memory (Demo Mode)

The app must never crash if MongoDB is missing

3️⃣ STORAGE ARCHITECTURE

Keep existing MemStorage

If MongoDB storage exists, wire it conditionally

Export one unified storage instance

Do NOT change route logic to know which DB is used

4️⃣ AI INTEGRATION (SAFE MODE)

Use OpenAI only if OPENAI_API_KEY exists

If AI key is missing:

Use fallback logic

Log once: AI enabled: false

AI features:

Recovery prediction (0–100)

Auto priority (Low / Medium / High)

Follow-up message generation

AI must NEVER block imports or crash server

5️⃣ ROUTES & IMPORT FIXES

Fix all broken imports (ERR_MODULE_NOT_FOUND)

Ensure:

routes.ts

aiService.ts

storage.ts

index.ts
all resolve correctly in tsx dev mode

Remove accidental imports like:

import { i } from "node_modules/..."

6️⃣ EXCEL UPLOAD STABILITY

Ensure Excel upload:

Never blanks the UI

Always returns JSON response

Logs upload status

Handle bad rows safely

7️⃣ SERVER START CONFIRMATION

On startup, console must show clear confirmation:

🚀 FedEx Assistant starting...
📦 Storage Mode: In-Memory (Demo Mode)   // OR MongoDB
AI enabled: true/false
🟢 MongoDB connected (if used)
[express] serving on port 5000

8️⃣ DO NOT DO THESE

❌ Do NOT remove existing features
❌ Do NOT rewrite frontend
❌ Do NOT change API contracts
❌ Do NOT introduce breaking changes

9️⃣ FINAL OUTPUT

Ensure npm run dev works

Ensure npm run build && npm start works

Fix only what is broken

Leave comments where changes were made

Treat this as a production stabilization task, not a rewrite.