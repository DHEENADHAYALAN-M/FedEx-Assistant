You are a senior full-stack engineer and code auditor.

Project context:
- This is a FedEx Assistant / DCA Management Platform
- Tech stack:
  - Node.js + TypeScript
  - Express backend
  - Vite frontend
  - In-memory storage (MemStorage) for demo
  - OpenAI integration (optional, fallback-based)
- The project must run cleanly in:
  - npm run dev (development)
  - npm run build + npm start (production)

Your mission:
DO NOT redesign the app.
DO NOT remove existing features.
ONLY fix, organize, and stabilize.

-------------------------
STEP 1 — Project Audit
-------------------------
• Scan the entire repository
• Identify:
  - broken imports
  - missing files
  - duplicate logic
  - unused imports
  - incorrect relative paths
  - TypeScript / ESM issues
  - dev vs prod mismatches

-------------------------
STEP 2 — Fix Runtime Errors
-------------------------
• Fix ERR_MODULE_NOT_FOUND issues
• Ensure all imports resolve correctly
• Ensure tsx works in dev mode
• Ensure dist/index.cjs works in production
• Ensure AI services fail safely when no API key exists

-------------------------
STEP 3 — Structure Validation
-------------------------
Ensure correct structure:

server/
 ├─ index.ts
 ├─ routes.ts
 ├─ storage.ts
 ├─ aiService.ts
 ├─ static.ts
 └─ vite.ts

• Ensure each file has ONE responsibility
• Ensure no circular dependencies
• Ensure routes.ts does not import unused modules

-------------------------
STEP 4 — AI Integration Safety
-------------------------
• OpenAI must be OPTIONAL
• If OPENAI_API_KEY is missing:
  - app must still run
  - fallback logic must be used
• Add clear console logs:
  - “AI enabled: true/false”
• Never block request flow if AI fails

-------------------------
STEP 5 — Storage Mode Confirmation
-------------------------
• Clearly log on startup:
  - “Storage Mode: In-Memory (Demo Mode)”
• Ensure seedDatabase() runs only once
• Ensure no duplicate seed entries

-------------------------
STEP 6 — API Validation
-------------------------
Verify these endpoints fully work:
• /api/dashboard/stats
• /api/dcas
• /api/cases
• /api/cases/import
• /api/admin/ai-status

Ensure:
• Proper HTTP status codes
• JSON responses
• No crashes on bad input

-------------------------
STEP 7 — Build & Dev Consistency
-------------------------
• npm run dev must work
• npm run build must succeed
• npm start must run dist/index.cjs
• PORT must default to 5000
• No Replit-specific code should break local VS Code use

-------------------------
STEP 8 — Security & Git Hygiene
-------------------------
• Ensure .env is NOT committed
• Add .env.example if missing
• Validate .gitignore correctness
• Do NOT store secrets in code

-------------------------
STEP 9 — Output Requirements
-------------------------
When finished:
1. List ALL fixes you made
2. Show corrected files only (not entire repo dump)
3. Confirm:
   - Dev works
   - Build works
   - Start works
4. Suggest next steps:
   - MongoDB migration (optional)
   - Deployment target (Railway / Render / Vercel)
   - AI feature expansion

IMPORTANT RULES:
• Keep existing logic
• Keep AI fallback behavior
• Keep MemStorage
• Fix only what is broken or unsafe
• Be minimal, clean, and production-safe
