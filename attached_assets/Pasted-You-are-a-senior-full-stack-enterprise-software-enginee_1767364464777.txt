You are a senior full-stack enterprise software engineer.

Build a COMPLETE, WORKING, PROFESSIONAL web application called
"DCA Management Dashboard" using Python Flask, HTML, CSS, JavaScript,
SQLite, and Chart.js.

This application is for an enterprise like FedEx to manage Debt Collection
Agencies (DCAs).

IMPORTANT:
- The system MUST be realistic, industrial-ready, and professional.
- The UI must look clean, corporate, and user-friendly.
- The app must run fully inside Replit with NO errors.
- Do NOT use external paid services.
- AI integration should be PLANNED via smart rule-based logic (AI-ready).

--------------------------------------------------
CORE FEATURES (MANDATORY)
--------------------------------------------------

1) EXCEL UPLOAD (SMART INGESTION)
- Admin can upload an Excel (.xlsx) file containing overdue cases
- Validate required columns:
  Case_ID, Customer_Name, Amount, Days_Overdue, Region, Status
- Reject invalid files with proper error messages
- Save valid data into SQLite database
- Maintain upload history (audit trail)

2) SMART AUTOMATION (NOT JUST UPLOAD)
After Excel upload, the system must automatically:
- Assign Priority:
    High → Amount > 50,000 OR Days_Overdue > 60
    Medium → Amount 20,000–50,000
    Low → Others
- Assign DCA automatically based on:
    - Region match
    - Current workload
- Set SLA deadlines automatically based on priority

3) CASE MANAGEMENT
- View all cases in a table
- Status lifecycle:
    New → Assigned → In Progress → Recovered / Escalated
- Update case status from UI
- Add notes/comments per case
- Every change must be logged (audit log)

4) DCA MANAGEMENT
- Maintain a DCA master table:
    DCA_Name, Region, Active_Cases, Recovered_Cases, SLA_Score
- Auto-update DCA performance based on case outcomes
- Show DCA ranking leaderboard

5) DASHBOARD (ENTERPRISE STYLE)
- KPI cards:
    Total Cases
    Pending Cases
    Recovered Cases
    SLA Breaches
- Charts using Chart.js:
    Cases by Status
    Cases by DCA
- Dashboard must auto-update when data changes

6) USER ROLES (SIMPLIFIED)
- Admin view (FedEx): full access
- DCA view: only assigned cases
(No real login required – simulate role via URL/page)

--------------------------------------------------
UI / UX REQUIREMENTS
--------------------------------------------------
- Clean professional layout
- Corporate colors (light background, blue/gray theme)
- Card-based dashboard
- Responsive layout
- Proper spacing, icons (optional)
- No ugly default HTML

--------------------------------------------------
TECHNICAL REQUIREMENTS
--------------------------------------------------
- Flask backend
- SQLite database (auto-create tables)
- Use pandas + openpyxl for Excel reading
- Chart.js for charts
- Modular code
- Clear comments in code
- Folder structure:

/app.py
/templates
  dashboard.html
  upload.html
  dca.html
/static
  style.css
  script.js

--------------------------------------------------
FUTURE-READY (VERY IMPORTANT)
--------------------------------------------------
- Clearly comment where AI / OpenAI API can be plugged in later
- Use rule-based logic now but design functions as:
    "ai_priority_engine()"
    "ai_assignment_engine()"
- Add comments explaining how AI will replace rules later

--------------------------------------------------
FINAL OUTPUT
--------------------------------------------------
- Provide FULL working code
- Ensure app runs without error on Replit
- Use port 81 and host 0.0.0.0
- This must look like a REAL enterprise product demo

Build the complete system now.
