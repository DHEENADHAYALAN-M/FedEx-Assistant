You are a senior backend engineer.

I have a working Node.js + Express project in Replit.
The project already runs, Excel upload works, and pages load correctly.

Your task is to add the FOUNDATIONAL OpenAI integration
in a secure and non-breaking way.

DO NOT add dashboards or complex AI logic yet.

------------------------------------------------
GOAL
------------------------------------------------
Prepare the project for AI features by:
- Integrating OpenAI SDK
- Handling API keys securely
- Adding one test AI endpoint
- Ensuring zero impact if API key is missing

------------------------------------------------
SECURITY REQUIREMENTS (MANDATORY)
------------------------------------------------
- OpenAI API key MUST be read only from:
  process.env.OPENAI_API_KEY
- Frontend must NEVER receive the API key
- No API key stored in database
- If key is missing → system must still run

------------------------------------------------
IMPLEMENTATION STEPS
------------------------------------------------

1) INSTALL OPENAI SDK
- Add official OpenAI npm package
- Do NOT use deprecated APIs

2) CREATE AI SERVICE FILE
Create:
server/ai/openaiService.js

This file must:
- Initialize OpenAI client safely
- Export a function:
  testAIConnection()

3) testAIConnection()
- If OPENAI_API_KEY is missing:
    return { enabled: false, message: "AI not configured" }
- If key exists:
    make a small, low-cost request
    return { enabled: true, message: "AI ready" }

4) ADD ADMIN-ONLY TEST ROUTE
Create:
GET /api/admin/ai-status

This route must:
- Call testAIConnection()
- Return JSON only
- Never expose API key
- Be safe to call from browser

5) ERROR HANDLING
- Wrap all OpenAI calls in try/catch
- No crash if OpenAI fails
- Log errors server-side only

------------------------------------------------
ABSOLUTE RULES
------------------------------------------------
❌ No frontend UI changes
❌ No Excel logic changes
❌ No AI business logic yet
❌ No database schema changes

------------------------------------------------
SUCCESS CONDITIONS
------------------------------------------------
- App starts even without OPENAI_API_KEY
- /api/admin/ai-status returns:
    { enabled: false } OR { enabled: true }
- No blank pages
- No crashes
- Clean foundation for future AI features

Apply the changes now.
