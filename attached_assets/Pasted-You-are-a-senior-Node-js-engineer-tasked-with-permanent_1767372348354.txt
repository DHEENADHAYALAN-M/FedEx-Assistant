You are a senior Node.js engineer tasked with permanently fixing
cross-platform startup and environment issues in an existing project.

DO NOT add new features.
DO NOT refactor business logic.
ONLY fix startup, environment, and stability issues.

------------------------------------------------
GOALS (MANDATORY)
------------------------------------------------
1. Fix the recurring Windows/Replit error:
   "'NODE_ENV' is not recognized as an internal or external command"

2. Ensure the project runs consistently on:
   - Windows (PowerShell / VS Code)
   - Replit
   - Linux / macOS
   - Cloud platforms (Railway, Render)

3. Make the fix PERMANENT (no manual steps required each run).

------------------------------------------------
REQUIRED FIXES
------------------------------------------------

A) FIX package.json SCRIPTS (CRITICAL)

- Update the start script to use cross-env.
- Ensure NODE_ENV is set correctly across all OS.

The final script MUST look like:

"scripts": {
  "build": "...(keep existing build command)",
  "start": "cross-env NODE_ENV=production node dist/index.cjs",
  "dev": "cross-env NODE_ENV=development node dist/index.cjs"
}

DO NOT use OS-specific syntax like:
  NODE_ENV=production
  set NODE_ENV=production

------------------------------------------------

B) ENSURE cross-env IS INSTALLED

- Add cross-env as a devDependency if not present.
- Use the correct version.
- Do not rely on global installs.

------------------------------------------------

C) ENVIRONMENT VARIABLE HANDLING

- Ensure the app reads environment variables ONLY via process.env
- Do NOT hardcode environment values
- Add a short comment explaining why cross-env is used

------------------------------------------------

D) AUDIT WARNING HANDLING (IMPORTANT)

- DO NOT remove xlsx
- DO NOT downgrade dependencies
- Add a README comment explaining:
  - xlsx has a known advisory
  - No upstream fix available
  - Risk is mitigated via controlled Excel uploads
  - This is acceptable for internal enterprise tools

------------------------------------------------

E) FINAL CHECKS

- The project MUST start with:
    npm start
- No NODE_ENV error should appear
- No manual environment commands should be required
- The fix must survive git clone + npm install + npm start

------------------------------------------------
OUTPUT REQUIREMENTS
------------------------------------------------
- Modify only necessary files (mainly package.json)
- Keep the project behavior unchanged
- Add minimal, professional comments
- Assume this project will be deployed later

Apply the fixes now.
