You are a senior full-stack engineer.

I have an existing Node.js + Express web application
where Excel upload works, but after upload the browser
shows a blank white page at /upload.

Your task is to FIX this issue permanently.

DO NOT add new features.
DO NOT refactor unrelated code.
ONLY fix the upload flow and routing behavior.

------------------------------------------------
PROBLEM DESCRIPTION
------------------------------------------------
- User opens /upload page
- Uploads Excel file
- Backend processes the file successfully
- Browser navigates to /upload and shows blank page

------------------------------------------------
ROOT CAUSE TO FIX
------------------------------------------------
- POST /upload returns JSON or redirects incorrectly
- Browser expects HTML but receives empty/JSON response
- Missing GET /upload route or incorrect response handling

------------------------------------------------
REQUIRED FIXES (MANDATORY)
------------------------------------------------

1) ENSURE GET /upload SERVES HTML
- Add a GET route for /upload
- It must serve upload.html from public folder
- No blank response allowed

Example behavior:
GET /upload → upload.html page

------------------------------------------------

2) FIX POST /upload RESPONSE
- POST /upload must NOT return HTML
- POST /upload must return JSON only

Example response:
{
  success: true,
  message: "Excel uploaded successfully",
  recordsProcessed: number
}

DO NOT use res.redirect() in POST /upload

------------------------------------------------

3) FIX FRONTEND UPLOAD FORM
- Prevent default form submission
- Use fetch() to submit FormData
- Handle JSON response properly
- Show success / error message
- Redirect only via JavaScript if needed

------------------------------------------------

4) USER EXPERIENCE REQUIREMENTS
- Page must NEVER go blank
- Upload success must show a message
- Errors must be visible in UI
- No full page reload after upload

------------------------------------------------
ABSOLUTE RULES
------------------------------------------------
❌ No page reloads after upload
❌ No res.redirect() in POST route
❌ No blank responses
✅ Clean API → UI separation

------------------------------------------------
SUCCESS CONDITIONS
------------------------------------------------
- Upload page stays visible
- Excel upload works
- User sees success message
- No white/blank page
- Works on Replit and local

Apply the fixes now.
