You are a senior Node.js engineer.

Integrate OpenAI AI features into an existing
enterprise DCA Management Platform.

DO NOT refactor existing business logic.
DO NOT change database schemas unless required for AI fields.
ONLY add a clean AI layer with safe fallbacks.

------------------------------------------------
AI GOALS
------------------------------------------------
1. AI Recovery Prediction (%)
2. AI Suggested Priority (High / Medium / Low)
3. AI Follow-up Message Generator

------------------------------------------------
TECH REQUIREMENTS
------------------------------------------------
- Use OpenAI official SDK
- Read API key from process.env.OPENAI_API_KEY
- If API key is missing or OpenAI fails → fallback to rule-based logic
- AI must NEVER break the app

------------------------------------------------
IMPLEMENTATION DETAILS
------------------------------------------------

A) Create a dedicated AI service file:
   server/ai/aiService.js (or .ts)

B) Implement these functions:

1) aiRecoveryPrediction(caseData)
   - Input: amount, daysOverdue, status
   - Output: number (0–100)
   - OpenAI prompt: ask for probability only
   - Fallback:
       score = 100
       - daysOverdue * 0.8
       - amount > 50000 ? 10 : 0
       clamp 0–100

2) aiSuggestedPriority(caseData)
   - AI suggests High / Medium / Low
   - Final priority = higher of:
       rule-based priority OR AI priority
   - Never downgrade risky cases

3) aiFollowUpMessage(caseData)
   - Generate a professional, polite reminder message
   - Do NOT auto-send
   - Message must be editable by user

------------------------------------------------
INTEGRATION POINTS
------------------------------------------------
- Call AI functions when:
  - Case is created (Excel upload)
  - Case is updated
- Store AI results in existing case record:
  - aiRecoveryScore
  - aiPriority
  - aiFollowUpMessage
  - aiLastUpdatedAt

------------------------------------------------
SAFETY & COMMENTS
------------------------------------------------
- Wrap all OpenAI calls in try/catch
- Add comments explaining:
  - Why AI is assistive
  - Why fallback exists
  - How this scales in production

------------------------------------------------
OUTPUT
------------------------------------------------
- Minimal code changes
- Clean, commented AI service
- App must still run without OPENAI_API_KEY
- No breaking changes

Apply AI integration now.
